# Test Cases

This directory holds human-readable YAML test cases provided by the user. Each
case describes an expected pipeline outcome and is executed by the evaluation
harness via `python -m regulator.evaluation`.
